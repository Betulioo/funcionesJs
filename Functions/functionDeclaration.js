//Functions in JavaScript
//Q1 - What is Function declaration

function circle(number) {
  return number * number;
}

// En JavaScript, la declaracion de una funcion es el proceso de definir una funcion para que esté disponible para su uso posterior en el codigo.Una funcion en Js es un bloque de codigo que realiza una tarea especifica cuando es invocada.

//invocando la funcion circle
circle(5);
