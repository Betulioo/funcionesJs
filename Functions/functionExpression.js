//Functions in JavaScript
//Q2 - What is Function Expression?

const circle = function (params) {
  return params * params;
};

//Es una forma de definir una funcion mediante la asignacion de una funcion a una variable o a una propiedad de un objeto.

//se crea una variable circle y se le asigna una funcion que en este caso es anonima(no tiene nombre).

//invocamos la funcion

console.log(circle(5));
